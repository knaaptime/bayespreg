name: bayespecon
channels:
  - conda-forge
dependencies:
  - libpysal
  - esda
  - formulaic
  - pymc
  - arviz
  - spreg
  - statsmodels
  - geopandas
  - geodatasets
  - arviz < 1  #pinned until supported by pymc
  - lazy_loader
  - fastprogress
  - scikit-sparse
  # optional
# - blackjax
# - numpyro
# - pip
#   - pip:
